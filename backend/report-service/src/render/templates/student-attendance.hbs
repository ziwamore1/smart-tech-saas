<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<title>Student Attendance Report - {{student.firstName}} {{student.lastName}}</title>
<style>
  @page { margin: 12mm 10mm; size: A4; }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', Arial, Helvetica, sans-serif; font-size: 12px; color: #111827; line-height: 1.4; }

  .student-header { background: {{secondaryColor}}; border-radius: 8px; padding: 14px 18px; margin-bottom: 16px; border-left: 4px solid {{primaryColor}}; }
  .student-header table { width: 100%; border-collapse: collapse; }
  .student-header td { padding: 3px 8px; font-size: 12px; }
  .student-header .label { color: #374151; font-weight: 700; width: 120px; }
  .student-header .value { font-weight: 700; color: #111827; }

  .section-title { font-size: 15px; font-weight: 800; color: {{primaryColor}}; margin: 16px 0 8px; padding-bottom: 4px; border-bottom: 2px solid {{primaryColor}}; }

  .stats-grid { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 16px; }
  .stat-box { flex: 1; min-width: 80px; background: #f3f4f6; border-radius: 6px; padding: 10px; text-align: center; border: 1.5px solid #9ca3af; }
  .stat-box .value { font-size: 23px; font-weight: 800; }
  .stat-box .label { font-size: 9px; color: #374151; text-transform: uppercase; margin-top: 2px; font-weight: 600; }

  .rate-high { color: #065f46; } .rate-mid { color: #9a3412; } .rate-low { color: #991b1b; }

  table.records { width: 100%; border-collapse: collapse; margin-top: 8px; }
  table.records th { background: #1f2937; color: #ffffff; padding: 6px 8px; font-size: 11px; font-weight: 700; text-align: center; text-transform: uppercase; border: 1px solid #111827; }
  table.records td { padding: 4px 8px; text-align: center; border: 1px solid #9ca3af; border-bottom-width: 1.5px; font-size: 11px; }
  table.records tr:nth-child(even) { background: #f3f4f6; }
  table.records .subject { text-align: left; }

  .status-badge { display: inline-block; padding: 1px 8px; border-radius: 8px; font-size: 9px; font-weight: 700; }
  .status-PRESENT { background: #d1fae5; color: #065f46; }
  .status-ABSENT { background: #fecaca; color: #991b1b; }
  .status-LATE { background: #fed7aa; color: #9a3412; }
  .status-EXCUSED { background: #bfdbfe; color: #1e40af; }
  .status-SICK { background: #fef08a; color: #854d0e; }
  .status-SUSPENDED { background: #e5e7eb; color: #374151; }
  .status-ACTIVITY { background: #e9d5ff; color: #5b21b6; }
  .status-PARTIAL_ATTENDANCE { background: #99f6e4; color: #0f766e; }

  .insight-box { background: #fffbeb; border: 1px solid #d97706; border-radius: 6px; padding: 10px 14px; margin: 12px 0; }
  .insight-box.warning { background: #fef2f2; border-color: #dc2626; }
  .insight-box.success { background: #f0fdf4; border-color: #16a34a; }

  .footer { margin-top: 20px; font-size: 10px; color: #4b5563; text-align: center; border-top: 1px solid #9ca3af; padding-top: 10px; }

  @media print {
    body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
  }
</style>
</head>
<body>

{{> header primaryColor=template.primaryColor secondaryColor=template.secondaryColor }}

<div class="student-header">
  <table>
    <tr><td class="label">Student Name</td><td class="value">{{student.firstName}} {{student.lastName}}</td>
      <td style="text-align:right;vertical-align:middle;" rowspan="3">
        {{#if student.photoUrl}}
        <img src="{{student.photoUrl}}" alt="{{student.firstName}}" style="width:64px;height:64px;border-radius:50%;object-fit:cover;border:3px solid #e5e7eb;" />
        {{else}}
        <div style="width:64px;height:64px;border-radius:50%;background:#e5e7eb;border:3px solid #6b7280;display:inline-flex;align-items:center;justify-content:center;font-size:18px;color:#374151;">—</div>
        {{/if}}
      </td>
    </tr>
    <tr><td class="label">Admission No.</td><td class="value">{{student.admissionNumber}}</td></tr>
    <tr><td class="label">Class</td><td class="value">{{student.className}}</td></tr>
    <tr><td class="label">Period</td><td class="value">{{startDate}} to {{endDate}}</td></tr>
  </table>
</div>

<div class="stats-grid">
  <div class="stat-box"><div class="value rate-{{rateClass}}">{{attendanceRate}}%</div><div class="label">Attendance Rate</div></div>
  <div class="stat-box"><div class="value" style="color:#059669;">{{stats.present}}</div><div class="label">Present</div></div>
  <div class="stat-box"><div class="value" style="color:#dc2626;">{{stats.absent}}</div><div class="label">Absent</div></div>
  <div class="stat-box"><div class="value" style="color:#ea580c;">{{stats.late}}</div><div class="label">Late</div></div>
  <div class="stat-box"><div class="value" style="color:#2563eb;">{{stats.excused}}</div><div class="label">Excused</div></div>
  <div class="stat-box"><div class="value" style="color:#ca8a04;">{{stats.sick}}</div><div class="label">Sick</div></div>
  <div class="stat-box"><div class="value" style="color:#6b7280;">{{stats.suspended}}</div><div class="label">Suspended</div></div>
  <div class="stat-box"><div class="value" style="color:#7c3aed;">{{stats.activity}}</div><div class="label">Activity</div></div>
  <div class="stat-box"><div class="value" style="color:#0d9488;">{{stats.partial}}</div><div class="label">Partial</div></div>
</div>

{{#if insights}}
<div class="section-title">AI Insights</div>
{{#each insights}}
<div class="insight-box {{type}}">
  <strong>{{title}}:</strong> {{text}}
</div>
{{/each}}
{{/if}}

<div class="section-title">Attendance Records</div>

<table class="records">
  <thead>
    <tr>
      <th>Date</th>
      <th>Subject</th>
      <th>Status</th>
      <th>Check In</th>
      <th>Check Out</th>
      <th>Late (min)</th>
      <th>Remarks</th>
    </tr>
  </thead>
  <tbody>
    {{#each records}}
    <tr>
      <td>{{date}}</td>
      <td class="subject">{{subject}}</td>
      <td><span class="status-badge status-{{status}}">{{statusLabel}}</span></td>
      <td>{{checkIn}}</td>
      <td>{{checkOut}}</td>
      <td>{{lateMinutes}}</td>
      <td>{{remarks}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>

<div class="footer">Generated on {{generatedAt}} | {{schoolName}}</div>

</body>
</html>
