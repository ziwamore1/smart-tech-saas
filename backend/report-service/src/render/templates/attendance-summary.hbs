<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<title>Attendance Summary - {{className}}</title>
<style>
  @page { margin: 12mm 10mm; size: A4; }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', Arial, Helvetica, sans-serif; font-size: 12px; color: #111827; line-height: 1.4; }

  .section-title { font-size: 15px; font-weight: 800; color: {{primaryColor}}; margin: 16px 0 8px; padding-bottom: 4px; border-bottom: 2px solid {{primaryColor}}; }

  .summary-grid { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 16px; }
  .summary-card { flex: 1; min-width: 100px; background: #f3f4f6; border-radius: 6px; padding: 10px 14px; text-align: center; border: 1.5px solid #9ca3af; }
  .summary-card .value { font-size: 22px; font-weight: 800; }
  .summary-card .label { font-size: 9px; color: #374151; text-transform: uppercase; letter-spacing: 0.5px; margin-top: 2px; font-weight: 600; }
  .summary-card.present .value { color: #065f46; }
  .summary-card.absent .value { color: #991b1b; }
  .summary-card.late .value { color: #9a3412; }
  .summary-card.excused .value { color: #1e40af; }
  .summary-card.sick .value { color: #854d0e; }
  .summary-card.rate .value { color: {{primaryColor}}; }

  table.students { width: 100%; border-collapse: collapse; margin-top: 8px; }
  table.students th { background: #1f2937; color: #ffffff; padding: 6px 8px; font-size: 11px; font-weight: 700; text-align: center; text-transform: uppercase; border: 1px solid #111827; }
  table.students td { padding: 5px 8px; text-align: center; border: 1px solid #9ca3af; border-bottom-width: 1.5px; font-size: 11px; }
  table.students tr:nth-child(even) { background: #f3f4f6; }
  table.students .name { text-align: left; font-weight: 600; }
  table.students .rate-high { color: #065f46; font-weight: 700; }
  table.students .rate-mid { color: #9a3412; font-weight: 700; }
  table.students .rate-low { color: #991b1b; font-weight: 700; }

  .insight-box { background: #fffbeb; border: 1px solid #d97706; border-radius: 6px; padding: 10px 14px; margin: 12px 0; }
  .insight-box.warning { background: #fef2f2; border-color: #dc2626; }
  .insight-box.success { background: #f0fdf4; border-color: #16a34a; }

  .footer { margin-top: 20px; font-size: 10px; color: #4b5563; text-align: center; border-top: 1px solid #9ca3af; padding-top: 10px; }

  @media print {
    body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
  }
</style>
</head>
<body>

{{> header primaryColor=template.primaryColor secondaryColor=template.secondaryColor }}

<h2 style="margin-bottom:4px;">Attendance Summary Report</h2>
<p style="font-size:12px; color:#374151;">{{className}} | {{startDate}} to {{endDate}}</p>

<div class="summary-grid">
  <div class="summary-card rate"><div class="value">{{overallRate}}%</div><div class="label">Attendance Rate</div></div>
  <div class="summary-card present"><div class="value">{{stats.present}}</div><div class="label">Present</div></div>
  <div class="summary-card absent"><div class="value">{{stats.absent}}</div><div class="label">Absent</div></div>
  <div class="summary-card late"><div class="value">{{stats.late}}</div><div class="label">Late</div></div>
  <div class="summary-card excused"><div class="value">{{stats.excused}}</div><div class="label">Excused</div></div>
  <div class="summary-card sick"><div class="value">{{stats.sick}}</div><div class="label">Sick</div></div>
</div>

{{#if insight}}
<div class="insight-box {{insight.type}}">
  <strong>Insight:</strong> {{insight.text}}
</div>
{{/if}}

<div class="section-title">Student Attendance Breakdown</div>

<table class="students">
  <thead>
    <tr>
      <th>#</th>
      <th>Student</th>
      <th>Adm No.</th>
      <th>Total</th>
      <th>Present</th>
      <th>Absent</th>
      <th>Late</th>
      <th>Excused</th>
      <th>Sick</th>
      <th>Rate</th>
    </tr>
  </thead>
  <tbody>
    {{#each students}}
    <tr>
      <td>{{inc @index}}</td>
      <td class="name">{{name}}</td>
      <td>{{admNo}}</td>
      <td>{{total}}</td>
      <td>{{present}}</td>
      <td>{{absent}}</td>
      <td>{{late}}</td>
      <td>{{excused}}</td>
      <td>{{sick}}</td>
      <td class="rate-{{rateClass}}">{{rate}}%</td>
    </tr>
    {{/each}}
  </tbody>
</table>

<div class="footer">Generated on {{generatedAt}} | {{schoolName}} | {{totalStudents}} students</div>

</body>
</html>
