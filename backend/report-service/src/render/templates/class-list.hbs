<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<title>Class List - {{className}}</title>
<style>
  @page { margin: 15mm 10mm; size: A4 landscape; }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', Arial, Helvetica, sans-serif; font-size: 12px; color: #111827; line-height: 1.4; }

  .school-header { text-align: center; margin-bottom: 18px; padding-bottom: 12px; border-bottom: 3px solid {{primaryColor}}; }
  .school-header h1 { font-size: 22px; font-weight: 800; color: {{primaryColor}}; margin: 0; }
  .school-header p { font-size: 12px; color: #374151; margin: 4px 0 0; }

  .report-title { text-align: center; margin-bottom: 14px; }
  .report-title h2 { font-size: 18px; font-weight: 800; color: #111827; margin: 0; }
  .report-title p { font-size: 12px; color: #374151; margin: 2px 0 0; }

  .summary-grid { display: flex; gap: 10px; margin-bottom: 16px; flex-wrap: wrap; }
  .summary-card { flex: 1; min-width: 100px; border-radius: 6px; padding: 10px 14px; text-align: center; border: 1.5px solid #9ca3af; }
  .summary-card .card-value { font-size: 21px; font-weight: 800; }
  .summary-card .card-label { font-size: 10px; color: #374151; text-transform: uppercase; letter-spacing: 0.5px; margin-top: 2px; font-weight: 600; }
  .summary-card.total { background: #f3f4f6; } .summary-card.total .card-value { color: {{primaryColor}}; }
  .summary-card.male { background: #dbeafe; } .summary-card.male .card-value { color: #1d4ed8; }
  .summary-card.female { background: #fce7f3; } .summary-card.female .card-value { color: #be185d; }
  .summary-card.capacity { background: #d1fae5; } .summary-card.capacity .card-value { color: #047857; }

  .info-bar { display: flex; gap: 20px; font-size: 12px; color: #374151; margin-bottom: 12px; padding: 8px 12px; background: #f3f4f6; border-radius: 6px; border: 1px solid #d1d5db; }
  .info-bar strong { color: #111827; }

  table.students { width: 100%; border-collapse: collapse; margin-top: 6px; }
  table.students th { background: #1f2937; color: #ffffff; padding: 7px 10px; font-size: 11px; font-weight: 700; text-align: center; text-transform: uppercase; letter-spacing: 0.4px; border: 1px solid #111827; }
  table.students td { padding: 5px 10px; text-align: center; border: 1px solid #9ca3af; border-bottom-width: 1.5px; font-size: 11px; }
  table.students tr:nth-child(even) { background: #f3f4f6; }
  table.students tr:hover { background: #e5e7eb; }
  table.students .left { text-align: left; }
  table.students .name { text-align: left; font-weight: 600; }

  .gender-male { color: #1d4ed8; font-weight: 700; }
  .gender-female { color: #be185d; font-weight: 700; }
  .status-active { color: #047857; font-weight: 700; }
  .status-inactive { color: #b91c1c; font-weight: 700; }

  .footer { margin-top: 18px; font-size: 10px; color: #374151; text-align: center; border-top: 1px solid #9ca3af; padding-top: 8px; }
  .footer .meta { display: flex; justify-content: space-between; }

  .page-break { page-break-after: always; }

  @media print {
    body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
  }
</style>
</head>
<body>

<div class="school-header">
  <h1>{{school.name}}</h1>
  {{#if school.address}}<p>{{school.address}}</p>{{/if}}
</div>

<div class="report-title">
  <h2>Class List - {{className}}</h2>
  <p>{{levelType}} | Generated on {{generatedAt}}</p>
</div>

<div class="summary-grid">
  <div class="summary-card total">
    <div class="card-value">{{summary.total}}</div>
    <div class="card-label">Total Students</div>
  </div>
  <div class="summary-card male">
    <div class="card-value">{{summary.male}}</div>
    <div class="card-label">Male</div>
  </div>
  <div class="summary-card female">
    <div class="card-value">{{summary.female}}</div>
    <div class="card-label">Female</div>
  </div>
  {{#if summary.capacity}}
  <div class="summary-card capacity">
    <div class="card-value">{{summary.capacity}}</div>
    <div class="card-label">Capacity</div>
  </div>
  {{/if}}
</div>

<div class="info-bar">
  <span><strong>Class:</strong> {{className}}</span>
  <span><strong>Level:</strong> {{levelType}}</span>
  <span><strong>Male:</strong> {{summary.male}}</span>
  <span><strong>Female:</strong> {{summary.female}}</span>
  <span><strong>Total:</strong> {{summary.total}}</span>
  {{#if summary.capacity}}<span><strong>Capacity:</strong> {{summary.capacity}}</span>{{/if}}
  <span><strong>M:F Ratio:</strong> {{summary.maleRatio}}:{{summary.femaleRatio}}</span>
</div>

<table class="students">
  <thead>
    <tr>
      <th style="width:36px;">#</th>
      <th style="width:50px;">Photo</th>
      <th style="width:90px;">Admission No</th>
      <th class="left">Full Name</th>
      <th style="width:70px;">Gender</th>
      <th style="width:110px;">Date of Birth</th>
      <th style="width:40px;">Age</th>
      <th style="width:70px;">Status</th>
    </tr>
  </thead>
  <tbody>
    {{#each students}}
    <tr>
      <td>{{add @index 1}}</td>
      <td>
        {{#if photoUrl}}
        <img src="{{photoUrl}}" alt="{{name}}" style="width:36px;height:36px;border-radius:50%;object-fit:cover;border:2px solid #e5e7eb;" />
        {{else}}
        <div style="width:36px;height:36px;border-radius:50%;background:#f3f4f6;border:2px solid #e5e7eb;display:flex;align-items:center;justify-content:center;font-size:12px;color:#9ca3af;">—</div>
        {{/if}}
      </td>
      <td>{{admNo}}</td>
      <td class="left name">{{name}}</td>
      <td class="gender-{{genderClass}}">{{genderLabel}}</td>
      <td>{{dob}}</td>
      <td>{{age}}</td>
      <td class="status-{{statusClass}}">{{status}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>

<div class="footer">
  <div class="meta">
    <span>{{school.name}}</span>
    <span>Page <span class="pageNumber"></span> of <span class="totalPages"></span></span>
    <span>{{generatedAt}}</span>
  </div>
</div>

</body>
</html>
